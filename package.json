{
  "name": "server",
  "version": "1.0.0",
  "description": "Server that gets data from the APIs",
  "main": "app.js",
  "scripts": {
    "start": "nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-npm:clean": "rm -rf node_modules/ && rm -rf package-lock.json && npm install && npm start"
  },
  "author": "jobu",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "express": "^4.17.1",
    "nodemon": "^1.19.4",
    "socket.io": "^4.1.2"
  },
  "optionalDependencies": {
    "bufferutil": "^4.0.3",
    "utf-8-validate": "^5.0.5"
  }
}
